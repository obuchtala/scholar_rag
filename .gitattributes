# Auto-detect text files and enforce LF
* text=auto eol=lf

# Mark binary files to avoid modification
*.png binary
*.jpg binary
*.exe binary

# Example override: force CRLF for Windows batch scripts
*.bat text eol=crlf